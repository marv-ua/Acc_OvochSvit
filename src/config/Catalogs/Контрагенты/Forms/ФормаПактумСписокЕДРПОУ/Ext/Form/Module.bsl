
&НаКлиенте
Процедура НажатиеОК(Команда)
	фОК=Истина;
	Закрыть(ЭтаФорма);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	РезультатПроверки = Пактум_Сервер.ПроверитьНаличиеПинкода();
	Если Не РезультатПроверки.Результат Тогда
		фОткрытьИнфо = Истина;
		ТекстОшибки = РезультатПроверки.ТекстОшибки;
		//ОбщегоНазначенияКлиентСервер.СообщитьПользователю(РезультатПроверки.ТекстОшибки);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если фОткрытьИнфо Тогда
		Отказ = Истина;
		ПередатьПараметры = Новый Структура("Инфо", ТекстОшибки);
		ОткрытьФорму("Обработка.Пактум_Настройка.Форма.ФормаИнфо", ПередатьПараметры);
	КонецЕсли;
КонецПроцедуры
