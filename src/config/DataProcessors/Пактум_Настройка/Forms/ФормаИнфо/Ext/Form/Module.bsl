
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Если Параметры.Свойство("Инфо") Тогда
	//	ИнфоТекст = Параметры.Инфо;
	//КонецЕсли;
	ИнфоТекст = НСтр("ru='Для автоматического заполнения карточек контрагентов в соответствии к введенному 
                      |набору кодов ЄГРПОУ (ДРФО) - подключите сервис ""Пактум.Контрагент"".';uk= 'Для автоматизованого заповнення карток контрагентів відповідно до введеного
                      |набору кодів ЄДРПОУ (ДРФО) - підключіть сервіс ""Пактум.Контрагент"".'");
КонецПроцедуры

&НаКлиенте
Процедура УзнатьБольше(Команда)
	ЗапуститьПриложение("https://portal.bas-soft.eu/applications/Pactum-Ukraine");
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	ЭтаФорма.Закрыть();
КонецПроцедуры




